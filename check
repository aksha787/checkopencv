import cv2
import numpy as np

print("OpenCV Version:", cv2.__version__)

try:
    # Try to create a blank image (basic test)
    blank_image = np.zeros((100, 100, 3), dtype=np.uint8)
    print("Successfully created a blank image.")

    # If you have a camera connected, uncomment the following lines to test it:
    # cap = cv2.VideoCapture(0) # 0 for the default camera
    # if not cap.isOpened():
    #     print("Error: Could not open video stream.")
    # else:
    #     ret, frame = cap.read()
    #     if ret:
    #         print("Successfully captured a frame from the camera.")
    #         # Optionally save the frame to see it
    #         # cv2.imwrite("captured_frame.jpg", frame)
    #     else:
    #         print("Error: Could not read frame from camera.")
    #     cap.release()

    print("OpenCV is working correctly!")

except Exception as e:
    print("An error occurred:", e)
    print("OpenCV might not be installed or configured correctly.")
